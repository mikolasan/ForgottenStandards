<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ImperialKeypad" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/gotham_pro_bold</item>
    </style>

    <style name="ImperialPanel" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/gotham_pro_reg</item>
    </style>

</resources>
